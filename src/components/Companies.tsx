import companies from "../assets/companies.png";

export default function Companies() {
  return (
    <section id="companies">
      <img className="w-full" src={companies} alt="company-tie-ups" />
    </section>
  );
}
